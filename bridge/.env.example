# Day One Bridge Service Configuration

# Port for the bridge service to listen on
PORT=3000

# Authentication token - must match the token configured in Cloudflare Worker
# Generate a secure random token: openssl rand -base64 32
AUTH_TOKEN=your-secure-token-here

# Path to Day One CLI executable
DAYONE_CLI_PATH=/usr/local/bin/dayone
